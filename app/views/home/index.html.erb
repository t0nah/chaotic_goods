<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .hero {
      background: url('https://via.placeholder.com/1920x500') center/cover no-repeat;
      height: 60vh;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
    }
    .hero h1 {
      font-size: 3rem;
      font-weight: bold;
    }
    .search-bar {
      margin-top: -50px;
      z-index: 2;
      position: relative;
    }
    .product-card {
      border: 1px solid #e0e0e0;
      border-radius: 10px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s, box-shadow 0.3s;
    }
    .product-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.15);
    }
    .product-image {
      height: 200px;
      object-fit: cover;
    }
    .btn-buy {
      background-color: #ff5722;
      color: white;
    }
    .btn-buy:hover {
      background-color: #e64a19;
    }
  </style>
  <title>Chaotic Goods - Home</title>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <a class="navbar-brand p-2" href="/">Chaotic Goods</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Navigation Links -->
      <ul class="navbar-nav ms-auto me-3">
        <li class="nav-item"><a class="nav-link" href="/products">Products</a></li>
        <li class="nav-item"><a class="nav-link" href="/brands">Brands</a></li>
        <li class="nav-item"><a class="nav-link" href="<%= page_path(1) %>">About</a></li>
        <li class="nav-item"><a class="nav-link" href="<%= page_path(2) %>>Contact</a></li>
        <li class="nav-item"><a class="nav-link" href="/cart"><i class="bi bi-cart"></i> Cart</a></li>
      </ul>
      <!-- Search Form -->
      <form class="d-flex" action="/products" method="get">
        <select class="form-select me-2" name="category" aria-label="Category">
          <option value="" selected>All Categories</option>
          <% @categories.each do |category| %>
            <option value="<%= category.id %>" <%= "selected" if params[:category] == category.id.to_s %>><%= category.name %></option>
          <% end %>
        </select>
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" name="query" value="<%= params[:query] %>">
        <button class="btn btn-outline-primary" type="submit">Search</button>
      </form>
    </div>
</nav>


  <!-- Hero Section -->
  <section class="hero text-center">
    <div>
      <h1>Welcome to Chaotic Goods</h1>
      <p>Refined affordable luxury</p>
    </div>
  </section>

  <div class="container py-5">
    <h2 class="mb-4 text-center">Featured Products</h2>
    <div class="row">
      <% @products.each do |product| %>
        <div class="col-md-3 mb-4">
          <div class="card product-card">
            <img src="<%= product.image_was %>" class="card-img-top product-image" alt="<%= product.name %>">
            <div class="card-body">
              <h5 class="card-title"><%= product.name %></h5>
              <p class="card-text text-muted"><%= truncate(product.description, length: 100) %></p>
              <p class="product-price"><%= number_to_currency(product.price) %></p>
              <a href="/products/<%= product.id %>" class="btn btn-buy">View Details</a>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <footer class="bg-dark text-white text-center py-3">
    <p>&copy; 2024 Chaotic Goods | All Rights Reserved</p>
  </footer>
</body>
</html>
